#!/bin/sh

[ "$0" = "./install" ] || echo "try './install'"
[ "$0" = "./install" ] || exit 99

./decrypt

for ii in $(ls -A ./credentials); do
    cp -r ./credentials/$ii ~
done

ssh-add

mkdir -r ${HOME}/git
src=git@github.com:bryanhann/kitchen
dst=${HOME}/git/kitchen

git clone ${src} ${dst}

